<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bigame.havadurumu">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NavigationMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".provience.IcAnadolu" />
        <activity android:name=".provience.Marmara" />
        <activity android:name=".provience.Ege" />
        <activity android:name=".provience.Akdeniz" />
        <activity android:name=".provience.Karadeniz" />
        <activity android:name=".provience.GuneyDoguAnadolu" />
        <activity android:name=".provience.DoguAnadolu" />
        <activity android:name=".info"/>
    </application>

</manifest>
